import React from "react";
import ModelViewerWrapper from "./components/ModelViewerWrapper";

const App = () => {
  return (
    <div>
      <ModelViewerWrapper />
    </div>
  );
};

export default App;